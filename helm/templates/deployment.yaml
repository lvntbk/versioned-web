apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "versioned-web.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "versioned-web.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "versioned-web.name" . }}
    spec:
      containers:
        - name: web
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
